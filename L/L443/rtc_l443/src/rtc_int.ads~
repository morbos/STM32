with Ada.Interrupts.Names; use Ada.Interrupts.Names;

package exti20 is

   protected Handler is
      pragma Interrupt_Priority;

   private

      procedure IRQ_Handler;
      pragma Attach_Handler (IRQ_Handler, _Interrupt);

   end Handler;

end BlueNrg_Int;
