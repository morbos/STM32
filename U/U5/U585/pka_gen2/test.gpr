with "Ada_Drivers_Library/boards/stm32u5_bluepill_ns.gpr";
with "Ada_Drivers_Library/boards/common_config.gpr";

project test extends "../../common.gpr" is

   for Languages use ("Ada");

   for Main use ("test.adb");

   for Source_Dirs use ("src/**");

   for Object_Dir use "obj/" & Common_Config.Build;

   for Target use "arm-eabi";

   for Runtime ("Ada") use STM32U5_Bluepill_NS'Runtime("Ada");

   package Builder is
     for Global_Configuration_Pragmas use "gnat.adc";
   end Builder;

end test;
