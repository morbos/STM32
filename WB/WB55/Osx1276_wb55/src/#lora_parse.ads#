with HAL;            use HAL;
with HAL.SPI;        use HAL.SPI;
package LoRa_Parse is

   type LoRa_Packet is new SPI_Data_8b (1 .. 255);

   procedure Parse (Buffer : LoRa_Packet; Len : Integer);

end LoRa_Parse;
